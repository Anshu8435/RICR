//  general function
let a = 20,
  b = 30;

function sum(x, y) {
  let z = x + y;
  console.log(z);
}

sum(a, b);

// 1.1. Create a simple function that prints "Hello World".
function message() {
  console.log("hello world");
}

message();

// 2. Write a function that displays your name in the console.

function nam() {
  console.log("Anshu Gupta");
}

nam();

// 3. Create a function that prints "Welcome to
// College".

function colnam() {
  console.log("welcone to college");
}

colnam();

// 4. Write a function that takes a name as a
// parameter and prints "Hello [name]".

function opt(name) {
  console.log(name);
}
opt("anshu ");

// 5. Create a function that takes two numbers and
// prints their sum.

function add(x, y) {
  return x + y;
}

let result = add(5, 3);

console.log(result);

let m = 5;
n = 5;

function sum(s, d) {
  let z = s + d;
  console.log(z);
}

sum(m, n);

// 6. Write a function that takes a number and
// prints its square.

function square(t) {
  return t * t;
}

console.log(square(5));

// 7. Create a function that takes two numbers and
// returns their addition.

function pls(h, k) {
  return h + k;
}

console.log(pls(5, 9));

// 8. Write a function that returns the
// multiplication of two numbers.

function squar(v, m) {
  return v * m;
}

console.log(squar(3, 9));

// 9. Create a function that takes a name and age
// and prints a message.

function name_mes(nam, age) {
  console.log(nam, age);
}
name_mes("anshu ", 22);

// 10.Write a function that returns the average of
// three numbers.

function average(e, f, g) {
  return (e + f + g) / 3;
}
console.log(average(5, 3, 8));

// 11. Create a function and call it multiple times
// with different values.




// 12. Build a function that takes two numbers and
// returns the greater number.


